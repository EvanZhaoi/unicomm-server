/**
 * 认证模块。
 *
 * <p>该模块负责把桌面端识别到的 Windows 用户和设备信息转换为后端可信身份，
 * 并通过 Sa-Token 签发业务接口可使用的访问令牌。</p>
 *
 * <p>当前职责边界：</p>
 * <ul>
 *   <li>桌面端用户认证入口。</li>
 *   <li>测试阶段人员源适配，以及未来真实企业人员 API 的适配边界。</li>
 *   <li>设备信任、首次设备自动绑定、新设备验证码。</li>
 *   <li>认证审计和用户快照维护。</li>
 * </ul>
 *
 * <p>业务模块不应硬编码默认用户名，也不应直接信任前端传入的 username；
 * 需要通过 Sa-Token 当前登录态获取用户上下文。</p>
 */
package com.unicomm.module.auth;
